import { View, Text } from "react-native";

export default function Active() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Active Task Screen</Text>
    </View>
  );
}